export class ProductGroup {
    
    // fields
    groupID: number;
    groupName: string;

    // constructor
    constructor(
        groupID:number,
        groupName:string) { 
            this.groupID = groupID; 
            this.groupName = groupName;
    }
}